# Kyverno Policy Platform

This directory holds the Argo-managed Kyverno install and the future policy definitions for the homelab platform.

Current contents:

- `values.yaml` for the Kyverno Helm release
- `policies/` for future Kyverno policy manifests

`IDP-7` intentionally installs Kyverno without adding baseline policies yet.
